# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT_EMAIL=mailto:alerts@example.com

# Optional: protects /api/cron/check from public calls.
# When deployed on Vercel, set this and Vercel Cron will send it automatically.
CRON_SECRET=
